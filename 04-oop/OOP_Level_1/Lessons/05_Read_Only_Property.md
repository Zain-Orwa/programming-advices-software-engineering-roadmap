# Read Only Properties:
    as we set the variable _ID as a private attributes(variable), we can initialize the id from the beginning or we can get from our file, and we create only getter property function without the set one so this gives us more scure and control over the code.
